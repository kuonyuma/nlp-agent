# Blueprint snapshots for exercise sessions

Exercise Sessions store an immutable snapshot of the selected blueprint at creation. This preserves a learner's active attempt and grading criteria when a teacher later edits or deletes the source blueprint; only newly created sessions use the newer blueprint.
