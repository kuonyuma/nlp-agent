DEEPSEEK_API_KEY="your-deepseek-api-key"
TAVILY_API_KEY=""

# Optional but recommended for stable WebUI sessions across service restarts.
# Use a random value of at least 32 bytes in non-development deployments.
NLP_AGENT_WEB_SECRET=""

# 可选：覆盖所有 Worker 的默认模型配置名。
# NLP_AGENT_WORKER_MODEL="deepseek-reasoner"
